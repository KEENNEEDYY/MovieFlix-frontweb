import './styles.css';

const Button = () => {
    return(
        <div className="btn-container">
            <button className="btn btn-primary">
                <p>sair</p>
            </button>
        </div>
    )
}

export default Button;